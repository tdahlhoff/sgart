<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Debug-only: allow cleartext http to the local dev stack (backend :8081, Keycloak :8080),
         scoped to the dev hosts in res/xml/network_security_config.xml. Release builds keep
         Android's TLS-only default (CLAUDE.md §5). -->
    <application android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
