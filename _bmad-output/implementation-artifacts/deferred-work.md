## Deferred from: planning of story-2.6 (2026-08-28)

- **„Zuordnung entfernen" (clear a store assignment back to unassigned) → post-MVP.** 2.6 is
  reassign-only (Timo, 2026-08-28): correcting a wrong store = pick the right one; the archived-store
  fallback (Story 1.8 E6 / 2.6 AC4) covers a store that vanishes. `ItemAssignedToStore` carries a
  **required** `storeId` — no null-store event. If members want to fully un-assign an item post-MVP,
  add either a nullable-store variant or a second `ItemStoreAssignmentCleared` event + a picker
  „Zuordnung entfernen" row. [Story 2.6 Cl. 3]
- **A moved item's store assignment does NOT travel to the target list.** Story 2.4's `ItemMovedToList`
  and `ItemMoveProcessManager` are unchanged (name/note/quantity only); a moved assigned item shows „+
  Geschäft" on the target and is re-assigned in one tap (Timo, 2026-08-28). If carrying the assignment
  across a move is wanted later, version `ItemMovedToList` (+ old-event codec tolerance) and widen the
  PM's target-side `AddItem` — coupled to the Epic-3 mid-move compensation defer. [Story 2.6 Cl. 4]
- **AC3's trip/print store-grouping consumers are Epic 3.** 2.6 makes the assignment durable +
  queryable (`item_read_model.store_id`) and proves it with a read-model test; the store-grouped trip
  view (Story 3.2) and print layout (Story 3.5) consume it. No trip/print UI is built in 2.6.
- **The reusable store picker (UX-DR22) is single-select in 2.6.** Story 3.1 (trip start, ≥1 store)
  extends it to multi-select; 3.2 (in-trip reroute) reuses it. Don't overbuild for those now (Cl. 8).

## Deferred from: dev-story of 2-2-listen-overview-with-offen-erledigt-filter (2026-08-26)

- **Item counts (Story 2.3) and check-off progress (Epic 3) on overview rows** — 2.2 ships the Offen/Erledigt filter and the Done archive shell only (LOCKED Clarification 1). Story 2.3 lights up a per-row item count in the row 2.2 builds; the progress bar (checked/total) lands in Epic 3 with check-off. No `item_count` column or count/progress UI exists yet — none should be added before the capability that produces the data.
- **Archived-unnamed-list label and a dedicated `DONE` status tint** — finalized in Epic 3 when Done lists become reachable for real. 2.2 uses the simplest correct fallback (`listsArchiveUnnamedFallback`, plain „Liste") for an unnamed archived row, and reuses `StatusLabelVariant.neutral` for the `DONE` label (a dedicated Done tint is Epic-3 polish). `app/lib/features/lists/presentation/lists_view.dart`, `app/lib/l10n/app_de.arb`.
- **`DONE`-rejects-rename / read-only-archive e2e coverage with a real Done list is Epic 3's** — 2.2's read-only invariant (AC2) is proven now only with synthetic `DONE` read-model rows (`ListDoneListsTest`, `ShoppingListControllerTest`, the Flutter widget tests) because no Epic-2 command drives a list out of `OPEN`. Epic 3 should add end-to-end coverage once the trip-completion event exists, alongside the reachable rename-rejection test noted in the 2.1 defer below.
- **`ListOpenLists`'s OPEN-only ordinal filter must include `IN_TRIP` in Epic 3** — reaffirming the 2.1 defer immediately below; 2.2 adds a sibling `DONE` filter (`ListDoneLists`) but does not change `ListOpenLists`'s own filter or the ordinal source.

## Deferred from: code review of 2-1-create-and-name-multiple-open-lists (2026-08-26)

- **`ListOpenLists` OPEN-only filter will under-count the "Liste N" ordinal once In-Trip is reachable** — the query's Javadoc and `V5` migration comment state the AC2 ordinal counts a household's Open **and later In-Trip** lists in creation order, but `ListOpenLists.forHousehold` filters to `status == OPEN` before the client derives the ordinal from array position. Consistent in Epic 2 (only `OPEN` is reachable); when Epic 3 makes `IN_TRIP` reachable, whoever feeds the ordinal must include In-Trip lists, or „Liste N" silently under-counts. `backend/.../application/query/ListOpenLists.java`.
- **Projector partial-projection robustness (log-and-skip + BIGSERIAL ordering)** — `ShoppingListReadModelProjector` log-and-skips a failed event and resubscribes `fromStart`. Two narrow, replay-recoverable consequences: (1) a skipped `ShoppingListCreated` leaves the following `ShoppingListRenamed` UPDATE hitting 0 rows (rename lost until the next replay); (2) because `sequence_number` is `BIGSERIAL` assigned at INSERT time, a skipped-then-later-projected create can invert creation order versus a create that projected first. Both self-heal on the next `fromStart` replay and need a transient per-event failure to trigger. If either becomes real at scale, derive ordering from the event's commit/stream position rather than insert-time BIGSERIAL, and reconsider silent skip for `Created`. `backend/.../adapter/out/ShoppingListReadModelProjector.java`.
- **✅ RESOLVED (2026-08-27) — `Household*Config` classes wire the collaboration context, not just households** — `HouseholdApplicationConfig`/`HouseholdReadModelConfig` also defined the shopping-list handlers, query, JDBC read model and projector beans; the class names no longer described their contents (§8 "the domain screams through the packages"). 2.1 reused them by spec direction. **Fixed before Story 2.3 dev via a standalone Boy-Scout rename** → `CollaborationApplicationConfig` / `CollaborationReadModelConfig` (git-mv, Javadocs updated, `StoreReferenceConfig`'s doc-ref updated). Behavior-preserving — Spring discovers them by `@Configuration` scan and bean identity is the `@Bean` method name, so no call-site changed; backend 271 tests green (unchanged). `backend/.../collaboration/adapter/out/`.

## Deferred from: dev-story of 2-1-create-and-name-multiple-open-lists (2026-08-26)

- **`ShoppingList.rename`'s `DONE`-rejects-rename branch has no reachable test yet** — `backend/src/main/java/de/sgart/collaboration/domain/ShoppingList.java` codes the guard (`rename` throws `ListNameChangeNotPermittedException` unless `status == OPEN`), but Epic 2's only status-producing event is `ShoppingListCreated` (→ `OPEN`); nothing drives a list to `DONE` until Epic 3 adds the trip-completion event. Epic 3 should add a unit test that rehydrates a synthetic `[ShoppingListCreated, …tripCompletionEvent]` history and asserts `rename` on the resulting `DONE` list throws — alongside the `IN_TRIP` transition's own coverage. See Story 2.1 Clarification 1.

## Deferred from: code review of 1-9-guided-onboarding-for-a-new-household (2026-08-24)

- **No client-side blank/whitespace/length guard on the onboarding name step** — `app/lib/features/onboarding/presentation/onboarding_wizard_page.dart:234` submits `controller.text` guarded only by `isSubmitting`; a blank/whitespace name makes a pointless backend round-trip (rejected inline as `household.nameRequired`) and there is no `maxLength`. Pre-existing pattern: the 1.6 `CreateHouseholdPage` (create_household_page.dart:85-87) behaves identically and `CreateHouseholdCubit.submit` does not early-return on an empty trimmed name. Fail-fast would add a client guard to both create paths.

## Deferred from: code review of 1-7-switch-select-rename-households (2026-08-24)

- **Concurrent-rename 409 (`concurrency.staleVersion`) is unmapped on the client** — `app/lib/shared/errors/error_message_resolver.dart` maps only `household.nameRequired` / `household.nameTooLong` / `household.renameNotPermitted`; a rename that loses the optimistic-concurrency race falls through to `errorGenericFallback` with no distinct "changed elsewhere, please retry" copy or retry affordance. Unreachable in Story 1.7 (every household has exactly one member — the creator — until Epic 4 introduces invites, so two concurrent renamers cannot exist), and offline/conflict UX is explicitly Epic 5. Note it; map or design the conflict copy when Epic 4/5 makes the path reachable.

## Deferred from: code review of 1-6-create-a-household-first-run-routing (2026-08-23)

- **KurrentDB idempotency check + append are not atomic** — `backend/src/main/java/de/sgart/collaboration/adapter/out/KurrentDbEventStore.java:47` reads the stream to check for an already-applied `commandId`, then appends under expected-version in a separate, non-atomic step (unlike `InMemoryEventStore`, whose `append` is `synchronized`). Two concurrent deliveries of the same `commandId` to the same existing stream can both pass the pre-check and both append; the loser gets `WrongExpectedVersionException` → `ConcurrencyConflictException` instead of the contract's silent no-op. Latent: not reachable via create-household (fresh stream per call); relevant only for future retry-against-existing-stream flows. The single-threaded shared contract test cannot catch it.
- **`NoPersistedPersonalDataTest` strips only line comments** — `backend/src/test/java/de/sgart/identity/NoPersistedPersonalDataTest.java` `withoutSqlComments` uses `replaceFirst("--.*$", "")`, so a future migration using a `/* … */` block comment containing "email"/"display name" would trip a false-positive PII-column failure. Test-only robustness gap, no current trigger.

## Deferred from: code review of 1-2-design-system-theming-foundation (2026-08-22)

- **No localization delegates while the UI already ships German copy** — `app/lib/main.dart:20-26` declares no `localizationsDelegates`, `supportedLocales`, or `locale`. On a `de-DE` device, Material's own strings (tooltips, dialog buttons, pickers, Scaffold a11y labels) resolve to English against German app copy. Belongs to Story 1.3 (localization layer, de-DE formatting).
- **`HomeCubit` hardening** — `app/lib/features/home/presentation/home_cubit.dart:11` emits without an `isClosed` guard, which becomes the norm-case failure once the offline queue and live sync add async completions after dispose; the state is a primitive `Cubit<int>`, which is the pattern later screens will copy in a DDD codebase; `bloc_test` is not a dev dependency despite BLoC-per-screen being the stated architecture; and `app/test/features/home/home_cubit_test.dart:7-17` closes its cubit in one test but not the other. Pre-existing Story 1.1 placeholder code, untouched by 1.2.
- **No top-level error boundary** — `app/lib/main.dart:11-13` is a bare `runApp` with no `FlutterError.onError`, `runZonedGuarded`, or `ErrorWidget.builder`, so any build-time exception shows the raw error screen and is captured nowhere. Pre-existing Story 1.1 scaffold; out of 1.2's client-only token/component scope.

## Deferred from: code review of 1-8-manage-stores-with-client-side-chain-matching (2026-08-24)

- Chain matcher matching-quality polish: short-chain prefix false positives (`"dm"` → „Dmitri's Kiosk") and word-boundary detection that recognises only ASCII space, not hyphen/comma (`"City-Edeka"` yields no Edeka suggestion). Advisory + clearable, so cosmetic. [app/lib/features/stores/domain/store_chain_matcher.dart]
- Archive command-id consistency: `StoresCubit.archiveStore` uses a fresh random `commandId` per call instead of a per-intent id; safe today only because the domain archive is a convergent no-op. [app/lib/features/stores/presentation/stores_cubit.dart:120]
- Aggregate store-id invariant (defensive): `Household.addStore` does not reject a reused/known `storeId`, and `apply(StoreAdded)` resurrects `archived=false` while the read-model upsert keeps `archived=true`. Unreachable via the shipped client (fresh UUIDs); the read-model behaviour is correct for redelivery. [backend/.../collaboration/domain/Household.java:119,176]

## Deferred from: code review of 1-11-personal-profile-screen (2026-08-25)

- Identity header text robustness: the display-name and email `Text` in `_IdentityHeader` have no `maxLines`/`overflow` — a long unbreakable email at a large OS text scale clips (bounded by `Expanded`, so no overflow exception and the 2x test passes, but it is not polished). Add `maxLines`/`TextOverflow.ellipsis` when touched. [app/lib/features/settings/presentation/profile_screen.dart:86]
- DSGVO logging guard: `email` (personal data) now rides on the app-wide emitted `AuthState` (symmetric with the pre-existing `displayName`). Safe today because there is no `BlocObserver`/transition logging. If auth-state logging is ever introduced, add a redacting `toString` so the email cannot leak into logs (CLAUDE.md §5). [app/lib/features/auth/presentation/auth_state.dart:33]

## Deferred from: code review of story-2.2 (2026-08-26)

- **Cached Done archive has no invalidation hook** — `ShoppingListsCubit.selectFilter` lazily loads the „Erledigt" archive once and caches it (`archiveStatus` idle→ready); nothing resets it to `idle` when a list transitions to Done. Latent in Epic 2 (the archive is empty-by-construction — no Epic-2 event produces a Done list). When Epic 3 adds trip-completion and a real Done transition becomes reachable, a newly-completed list won't appear in the archive until a full screen reload. Add an invalidation path (reset `archiveStatus` to `idle`, or refetch) when the first Done-producing event lands in Epic 3. [app/lib/features/lists/presentation/shopping_lists_cubit.dart:45]

## Deferred from: code review of story-2.3 (2026-08-27)

- **Edit unit fallback silently coerces an unknown server unit to PIECE** — `_ItemFormSheetBodyState` initialises `_selectedUnit = _unitFromServerName(existing.unit) ?? formatting.Unit.piece`; on edit, a server unit outside the client `Unit` enum silently becomes PIECE on save, corrupting the quantity. Latent: the Dart and Java `Unit` enums (`PIECE/GRAM/KILOGRAM/MILLILITRE/LITRE/PACK`) match exactly today, so unreachable. Add a guard (disable save / keep raw unit) if the vocabularies ever diverge. [app/lib/features/lists/presentation/item_form_sheet.dart:41]
- **`DELETE /items/{itemId}` carries a JSON request body** — `ItemController.remove` binds `@RequestBody RemoveItemRequest` and the client sends `{'commandId': …}` via `deleteJson`. DELETE bodies are discouraged by the HTTP spec and stripped by some proxies/clients; the `commandId` would then arrive null and fail envelope validation. Mirrors the pre-existing archive endpoint pattern, so a latent portability risk rather than a regression. [backend/.../collaboration/adapter/in/ItemController.java]
- **`item_read_model` has no FK / `ON DELETE` relationship to the list/household** — V6 stores `list_id`/`household_id` as bare `UUID NOT NULL` with only a plain index. When list deletion or household erasure (Epic 6, anticipated by the migration's own denormalised-`household_id` comment) arrives, orphaned item rows won't be cleaned up automatically. Add the cascade/cleanup path with Epic-6 erasure. [backend/.../resources/db/migration/V6__item_read_model.sql]
- **`quantity_amount NUMERIC` has no precision/scale bound** — combined with unbounded `new BigDecimal(rawAmount)` on the write side (only `signum()>0` validated), an arbitrarily large/high-scale decimal can be persisted. No functional break; add a domain ceiling / column bound when convenient. [backend/.../resources/db/migration/V6__item_read_model.sql]

## Deferred from: code review of story-2.4 (2026-08-27)

- **PM `DuplicateItemException` swallow silently discards the moved item's quantity** — in the stale-client-pre-check race, `ItemMoveProcessManager.onItemMovedToList` takes the clean-move path, `target.addItem` throws `DuplicateItemException`, and it is swallowed as convergent success: the source item is removed but the member's merge intent (their quantity) is lost with only a `log.debug`. Spec-accepted (Cl. 3, "the quantity prompt simply does not appear in that rare race") and coupled to the Epic-3 mid-move compensation defer. Revisit alongside that compensation work. [backend/.../collaboration/application/ItemMoveProcessManager.java:70]
- **PM subscription never cancels its KurrentDB `Subscription` on `stop()`** — `CollaborationProcessManagerSubscription.subscribe()` discards the handle returned by `subscribeToAll`; `stop()` only shuts the resubscribe scheduler, so the subscription keeps firing `react(...)` until the client itself closes. Pre-existing pattern — `ShoppingListReadModelProjector` has the identical leak — so fix both consistently rather than one-off. [backend/.../collaboration/adapter/out/CollaborationProcessManagerSubscription.java:102]
- **`MoveItemRequest` (and its sibling write DTOs) have no null/body validation → 500 not 400 on an empty body** — `ItemController.move` dereferences `request.targetListId()`/`request.commandId()` with no `@Valid`/null guard; a `POST .../move` with a missing or empty JSON body NPEs into a 500. Consistent with every existing write endpoint on this controller (none is `@Valid`d), so harden the controller as a group. [backend/.../collaboration/adapter/in/ItemController.java:146]

## Deferred from: planning of story-2.5 (2026-08-27)

- **`default Store` prefill + suggestion store chip → Story 2.6.** Story 2.5 AC2 lists prefill of "unit, note, default Store" and `screen-list-detail.html` draws a store chip / „zuletzt Edeka" on each suggestion, but store-on-item does not exist until Story 2.6 (no store column on `item_read_model`, no assignment command/event). 2.5 records + prefills name/unit/note only; 2.6 extends `item_suggestion_read_model`, the suggestion rows, and the prefill with the last-used store. Premature-value rule (Epic-1 retro). [Story 2.5 Cl. 4]
- **Suggestion recency ranking + fuzzy/typo tolerance.** 2.5 matches by case-insensitive prefix on the trimmed name and orders alphabetically (replay-stable). "prefix/fuzzy" in the AC is satisfied by prefix for MVP; add frequency/recency ranking (needs a last-used ordering key that survives replay) and typo tolerance when the suggestion set grows enough to warrant it. [Story 2.5 Cl. 6]
- **Live-sync refresh of the client suggestion cache → Epic 4 (SSE).** 2.5 fetches the household suggestion set once per list-detail open and optimistically upserts the local cache on a successful add/update (read-your-writes). A peer's add on another device won't appear in this device's autocomplete until the next open. Wire cache invalidation/refresh into the Epic-4 live-sync stream. [Story 2.5 Cl. 2]
- **`ItemUpdated` still carries no `householdId`.** 2.5's projector resolves the household for the suggestion upsert via `ItemReadModel.householdIdOf(itemId)` rather than versioning the Story 2.3 event. If a future story needs `householdId` on `ItemUpdated` for another reason, add it there (with old-event tolerance in the codec) and drop the lookup. [Story 2.5 Cl. 5]

## Deferred from: code review of story-2.5 (2026-08-27)

- **No shared transaction across the projector's two read-model writes** — `project(ItemAdded)` runs `itemReadModel.insertItem(...)` and `itemSuggestionReadModel.recordUsage(...)` as two independent autocommit statements, and the subscription logs-and-skips a failing event. If the second write fails, `item_read_model` and `item_suggestion_read_model` diverge for that event with no operator signal. Pre-existing projector design (no transaction boundary since Story 2.1) — wrap one event's projection in a single transactional step when the projector gets checkpointing. [backend/.../collaboration/adapter/out/ShoppingListReadModelProjector.java:90]
- **`normalized_name VARCHAR(120)` exactly equals `ItemName.MAX_LENGTH`** — `recordUsage` inserts `name.trim().toLowerCase(Locale.ROOT)` into a column the same width as the source name, but Unicode lowercasing can lengthen a string (e.g. `İ`). A pathological 120-char name would fail the insert, be logged-and-skipped, and fail again on every replay. Vanishingly rare for German grocery content; widen the column (or truncate/hash the key) if it ever bites. [backend/.../resources/db/migration/V7__item_suggestion_read_model.sql]
- **The suggestion panel is inline layout, not a floating overlay** — `_SuggestionPanel` is a plain sibling above the `TextField` in the page `Column` while the item list sits in an `Expanded`, so opening the panel (up to 280px) shrinks the visible item list instead of floating over it. Task 10 says "overlay/panel that rises ABOVE the field"; functionally "above" as AC4 requires, but not the artifact's State B overlay. Revisit if the shrink proves jarring on a small screen. [app/lib/features/lists/presentation/fast_add_field.dart:98]
- **`actionError` renders at the bottom of the scrolling item list** — a fast-add rejection (e.g. a duplicate) shows inside `_ReadyBody`'s scroll view, so on a list longer than one screen the message can land off-screen while the field that triggered it sits pinned at the bottom, making the add look like it silently did nothing. Pre-existing Story 2.3 placement, newly more visible now that the add surface is the bottom field. [app/lib/features/lists/presentation/list_detail_page.dart:99]
- **Let a member remove an article from the autocomplete history (post-MVP)** — `item_suggestion_read_model` is upsert-only by design (Cl. 1): nothing ever deletes a row, `suggestionsOf` has no `LIMIT`, so the table grows monotonically per household and its whole lifetime content ships on every list-detail open. A note the member deliberately removed also keeps being re-attached by the prefill, and erasure exists only at household level (Epic 6). Decision (Timo, 2026-08-27): keep it growing for now — a household's distinct-article count is not expected to grow fast enough to matter pre-MVP. Post-MVP, add a member-facing "remove this article from suggestions" action; that same delete path becomes the item-level erasure/retention answer for CLAUDE.md §5, and a payload cap can follow if it is ever needed. [Story 2.5 Cl. 1; backend/.../adapter/out/JdbcItemSuggestionReadModel.java:31; backend/.../db/migration/V7__item_suggestion_read_model.sql]

## Deferred from: planning of story-2.6 (2026-08-28)

- **"Zuordnung entfernen" (clear-to-unassigned) — post-MVP.** `ItemAssignedToStore` carries a required `storeId`; there is no null-store event/command in MVP (Cl. 3). Correcting a wrong assignment means reassigning to the right store (last-wins) — the epic ACs require only assign + inline-create, and the archived-store fallback (AC4) already covers a store that later vanishes. Decision (Timo, 2026-08-28): add a member-facing "Zuordnung entfernen" action post-MVP if wanted (a new nullable-`storeId` event, or a sentinel, mirroring the `ItemAssignedToStore` shape). [Story 2.6 Cl. 3; backend/.../collaboration/domain/event/ItemAssignedToStore.java]
- **Picker multi-select for trip start (Story 3.1) is not built here.** `store_picker_sheet.dart` is deliberately single-select, returning one `StoreSummary` (Cl. 8) — Story 3.1 (trip start, needs ≥1 store) extends the same reusable sheet rather than duplicating it. [Story 2.6 Cl. 8; app/lib/features/stores/presentation/store_picker_sheet.dart]

## Deferred from: code review of story-3.2 (2026-08-29)

- **Cross-projector transient — `GET /active` can return a `tripId` with an empty store set.** `active_trip_id` (list projector, on `TripStartedForList`) and `trip_store_read_model` (the separate new trip projector, on `TripStarted`) are independently eventually-consistent; right after a trip starts (and while the client's active-stores load is in flight) every item renders under „Noch nicht zugeordnet" with empty groups, and the client has no settle/retry affordance. Inherent to the CQRS/ES model — candidate: a client-side retry or "trip still loading" state when active-but-no-stores. (`TripView.java`, `trip_cubit.dart` bootstrap, `trip_state.dart:82-94`)
- **Add-store endpoint ignores the `{listId}` path segment.** `POST /households/{h}/lists/{listId}/trips/{tripId}/stores` never validates the trip belongs to `listId` (the handler resolves via household + tripId only), unlike the list-scoped `GET .../active`. URL contract looseness — membership remains the real authorization gate. (`TripController.java:81-93`)
- **`ShoppingTripReadModelProjector` replays `fromStart` on every start/reconnect.** No checkpoint / stored subscription position, so each app (re)start and each dropped-connection resubscribe re-reads all `trip-` streams from zero — O(all trip events). Correct (idempotent `ON CONFLICT DO NOTHING` upserts) and mirrors the existing `ShoppingListReadModelProjector`, so this is a repo-wide scalability debt (checkpointed subscriptions), not unique to 3.2. (`ShoppingTripReadModelProjector.java:141`)
