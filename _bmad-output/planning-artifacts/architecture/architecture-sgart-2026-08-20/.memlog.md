---
scope: SGART — collaborative, privacy-first (DSGVO) household grocery & receipt-tracking app. Feature-altitude spine covering the MVP (Household, ShoppingList, ShoppingTrip) with Post-MVP (Receipt, Product/price intelligence) shape reserved.
purpose: build-substrate
altitude: feature
updated: 2026-08-20T19:12
---

- (decision) [ADOPTED] Paradigm: DDD + CQRS + Event Sourcing on the backend; hexagonal (ports & adapters) so domain stays free of infrastructure. Binds: all backend. Prevents: business rules leaking into controllers/persistence, and read/write concerns entangling. Settled in SGART.md/PRD.
- (decision) [ADOPTED] Write model = EventStoreDB (immutable event streams as source of truth); Read model = PostgreSQL projections. Binds: all backend state. Prevents: two owners of truth / mutable-state drift. Settled in SGART.md.
- (decision) [ADOPTED] Identity fully delegated to Keycloak (self-hosted). Backend extracts keycloakUserId from JWT; never accepts user id in request body/path. Binds: all auth. Prevents: SGART storing credentials / spoofable identity. Settled in SGART.md/PRD FR-3.
- (decision) [ADOPTED] Real-time propagation via Server-Sent Events (SSE); Flutter maps domain/read events to BLoC. Push notifications are content-free pings (FCM/APNs) that only prompt an authenticated fetch. Binds: live sync, notifications. Prevents: grocery/personal data traversing third-party push infra. Settled PRD FR-10/FR-19.
- (constraint) [ADOPTED] Hosting: Docker on German/EU server (e.g. Hetzner/IONOS); NO US cloud anywhere in the data path, ever. Full DSGVO posture. Binds: all infra/provider choices. Settled PRD §5 Non-Goals.
- (decision) [ADOPTED] Frontend = Flutter (iOS+Android) with BLoC state management. Web exists only as invite-acceptance fallback in v1, not a daily client. Binds: client. Settled PRD §6.
- (decision) [ADOPTED] OCR (Post-MVP) behind a Strategy/port abstraction: start on-device (ML Kit), swappable to self-hosted/EU provider. Binds: Price Intelligence context. Prevents: coupling receipt flow to one OCR vendor / a US OCR cloud. Settled PRD FR-20.
- (constraint) MVP aggregate scope = Household, ShoppingList, ShoppingTrip only. Receipt & Product aggregates are Post-MVP shape, reserved but not built. Binds: build scope. Source PRD §6 / addendum B.
- (decision) Bounded contexts (as modules in ONE modular-monolith Spring Boot deployable, not microservices): (1) Collaboration [core] = Household+Member+Store+ShoppingList+ShoppingTrip aggregates; Household and Shopping intentionally NOT split for MVP (shared ubiquitous language + consistency neighborhood, solo build). (2) Store Reference [supporting] = StoreChain, per-market seed, Post-MVP geo; read-mostly. (3) Price Intelligence [core, Post-MVP] = Receipt+Product+PriceObservation; integrates with Collaboration only via async domain events (TripCompleted/ReceiptConfirmed). (4) Identity [generic] = Keycloak via Anti-Corruption Layer. Binds: whole system structure. Prevents: two owners of one entity across modules, and MVP core depending on Post-MVP price code. User-confirmed.
- (direction) User explicitly rejected microservices and any Household/Shopping split for this app size; keep boundaries as code modules only.
- (decision) Identity/DSGVO: events reference a person ONLY by a household-scoped pseudonymous MemberId (per-membership, chosen for cross-household unlinkability). keycloakUserId, email, display name NEVER enter an event stream. The Identity ACL owns the sole mutable mapping {householdId, memberId -> keycloakUserId} and resolves (keycloakUserId, householdId)->memberId per request. Binds: every aggregate/event that references a person. Prevents: PII embedded in the immutable log; cross-household correlation of a person. User-confirmed.
- (decision) Display name/email are NEVER persisted by SGART; resolved on demand from Keycloak/JWT claims for display only. Binds: all read models/UI. Prevents: stale PII copies needing erasure. (Hardens PRD FR-3 assumption.)
- (decision) Invite email never enters an event: MemberInvited carries inviteId + salted HMAC(normalized email) for the no-duplicate-pending-invite invariant; raw email lives only in a mutable invite side-store, purged on accept/expire/erasure; delivery via Keycloak. Binds: Household invite flow. Prevents: raw email PII in immutable log while preserving the duplicate invariant.
- (decision) Right-to-erasure = destroy the person's Identity-ACL mapping rows + scrub PostgreSQL read models + purge device/offline caches + delete Keycloak account. Event log is NEVER rewritten; residual orphaned pseudonyms are unlinkable => anonymized under GDPR Recital 26 (posture user-owned). Binds: erasure workflow across all contexts. Prevents: event-history rewriting / non-compliant retention.
- (decision) Crypto-shredding held in RESERVE (not built for MVP) for content that is itself PII inside an event/blob: Post-MVP receipt images / OCR raw text encrypted with a per-subject data key in a vault; erasure destroys the key. Binds: Price Intelligence context (Post-MVP). Prevents: unerasable PII in receipt payloads.
- (decision) Value objects (immutable): Money = integer minor units (cents) + ISO currency code, NEVER floating point (MVP EUR-only but currency explicit); Quantity = amount + Unit from a controlled, extensible vocabulary (piece/gram/kilogram/millilitre/litre/pack...) NOT free text; plus StoreAssignment, Locale, Fingerprint, EmailHash. Binds: all money/quantity handling across contexts. Prevents: float money bugs, unanalysable free-text units blocking price-per-unit comparison. User-confirmed.
- (decision) Item is an entity INSIDE the ShoppingList aggregate (has itemId); Store is an entity INSIDE the Household aggregate. Neither is referenced or mutated from outside its aggregate root. Binds: aggregate boundaries. Prevents: two owners of one entity; direct cross-aggregate mutation.
- (decision) Ubiquitous-language fix for the 'Member' overload: entity = Member (a person's participation in a household); role = HouseholdRole { Admin, Participant } (was ADMIN|MEMBER). Rename ripples through events (MemberInvited.role, MemberRoleChanged.newRole) and the roles/permissions table. Binds: Household aggregate + all role checks. Prevents: one word ('Member') meaning both participation and role. User-confirmed (Admin/Participant).
- (decision) State-mutation invariant: every change = command -> aggregate -> event(s) appended to EventStoreDB with an expected-version check (optimistic concurrency). Read models (PostgreSQL) are projections only, never written directly; projections are eventually consistent. Binds: all backend writes/reads. Prevents: direct read-model mutation, two write paths, lost updates. User-confirmed.
- (decision) Offline queue rides the mutation invariant: each queued command carries (based-on aggregate version, client command-id). Replay: stale expected-version -> reject -> client surfaces coarse keep/discard (FR-26); command-id makes replay idempotent (no double-apply). Delivers FR-11 + FR-26 + pending-count UX. Binds: offline sync + all command handlers. Prevents: silent LWW, double-apply on retry. User-confirmed.
- (decision) RECONCILE (PRD wins over stale SGART.md/addendum): conflict handling = FR-26 bounded surfacing, NOT last-writer-wins (drop addendum H 'best-effort LWW' wording). Binds: offline/sync. User-confirmed.
- (constraint) MVP ShoppingTrip is Active->Done only (FR-15 pause/resume deferred to backlog); invariant = at most one ACTIVE trip per list. TripPaused/TripResumed events reserved, not built. Cross-day shopping = complete + transfer leftovers to new list. User-confirmed.
- (constraint) MVP notifications = fixed default set; NotificationSettingsUpdated (per-member config) is Post-MVP, event reserved not built. User-confirmed.
- (decision) Uniform error model: {code, message, details}. code = client-facing machine key (drives localized UI copy client-side); message = log/debug only, never shown to end user. Binds: all API responses. Source PRD/addendum.
- (decision) Transport: REST under /api/v1 (JWT from Keycloak; keycloakUserId from token, never in body/path) + SSE per-household stream for live sync. No pagination in MVP (full result sets). Binds: all client-server. Source addendum E.
- (version) Web-verified Aug 2026: Java 25 LTS (Sep 2025). Spring Boot 4.1.0 (Jun 2026, on Spring Framework 7, Java 17+ supports to 26). Flutter 3.44 stable (May 2026). Keycloak 26.7.0 (Jul 2026; NO LTS - always run newest). PostgreSQL 18.6 (Aug 2026).
- (version) IMPORTANT rebrand: EventStoreDB -> KurrentDB (company Kurrent). Pin KurrentDB 25.x line (docs v25.1); yearly LTS ~January, LTS uses minor .0 from 26.0. Seed value - confirm current LTS line at build time. All SGART.md/addendum references to 'EventStoreDB' now mean KurrentDB.
- (decision) Reviewer-gate tightening (adversarial lens): AD-5 - Identity ACL is the SOLE minter of MemberId (at invite acceptance); MemberJoined carries that id, no other component mints one. Prevents: two components generating divergent member ids.
- (decision) Reviewer-gate tightening: AD-6 - invite email HMAC uses a STABLE per-deployment server secret, never a per-invite random salt (a random salt would break the no-duplicate-invite invariant). Prevents: silent duplicate-invite check failure.
- (decision) Reviewer-gate tightening: AD-8 - the offline concurrency token is the TARGET aggregate ROOT's stream version specifically (never a related aggregate's). Prevents: handlers disagreeing on which version gates the write.
- (decision) Reviewer-gate tightening: AD-10 - process-manager-issued commands are idempotent via a command-id derived deterministically from the triggering event id; AD-8 idempotency covers both client- and PM-originated commands. Prevents: double-apply (e.g. double postpone) on subscription/projection replay.
- (event) Reviewer Gate run at Finalize: lint_spine.py clean (3 low findings were intentional stream-key {id} pattern placeholders); version-verification lens passed (all stack pins web-verified 2026-08-20, KurrentDB flagged confirm-at-build); adversarial lens found 4 divergence gaps, all fixed in-spine (AD-5/6/8/10).
- (event) spine finalized
- (decision) UPDATE: Trip pause/resume DROPPED entirely (not merely deferred). ShoppingTrip lifecycle is permanently Active->Done; TripPaused/TripResumed events are removed from the reserved set. Cross-day/unfinished shopping is fully handled by completing the trip and transferring remaining items to the next list (FR-16). User: 'I don't see any need for pausing/resuming.' Supersedes prior 'reserved-not-built' treatment and overrides PRD FR-15 (currently backlog). Binds: ShoppingTrip aggregate. Prevents: a Paused state and its 'at most one Active-or-Paused' variant ever entering the model.
- (event) spine updated (pause/resume dropped); re-lint clean apart from intentional stream-key placeholders; deck updated
