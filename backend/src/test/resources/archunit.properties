# The architecture rules are permanent guardrails written before the code they guard exists.
# While a rule's target packages are still empty (fresh scaffold), evaluating it checks zero
# classes; that is expected here and must not fail the build. Each rule starts enforcing
# automatically as soon as the first matching class is added by a later story.
archRule.failOnEmptyShould=false
