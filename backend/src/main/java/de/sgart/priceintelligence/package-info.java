/**
 * CORE context, POST-MVP — Receipt, Product, price observations. Reserved-empty in the MVP: integrated later only via async domain events, never by reaching into another context (AD-2). No aggregates yet (YAGNI).
 */
package de.sgart.priceintelligence;
